<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ekip Üyesi Düzenle</title>
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #fff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    
    h1 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 1.5rem;
      color: #4caf50;
    }
    
    form {
      background-color: #1f1f1f;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
      width: 100%;
      max-width: 500px;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      align-items: center;
    }
    
    form input[type="text"],
    form input[type="url"],
    form textarea,
    form button {
      width: 80%;
      padding: 0.8rem;
      border: none;
      border-radius: 4px;
      font-size: 1rem;
    }
    
    form input[type="text"],
    form input[type="url"],
    form textarea {
      background-color: #2c2c2c;
      color: #fff;
    }
    
    form textarea {
      resize: vertical;
      min-height: 100px;
    }
    
    form button {
      background-color: #4caf50;
      color: #fff;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    
    form button:hover {
      background-color: #45a049;
    }
    
    form input[type="text"]::placeholder,
    form input[type="url"]::placeholder,
    form textarea::placeholder {
      color: #ccc;
    }
    
  </style>
</head>
<body>
  <h1>Ekip Üyesini Düzenle</h1>
  <form action="/admin/about/edit/<%= member._id %>" method="POST" class="form">
    <input type="text" name="name" value="<%= member.name %>" placeholder="İsim" required>
    <input type="text" name="role" value="<%= member.role %>" placeholder="Rol" required>
    <textarea name="bio" placeholder="Biyografi" required><%= member.bio %></textarea>
    <input type="url" name="kick" value="<%= member.kick %>" placeholder="Kick URL">
  <input type="url" name="twitter" value="<%= member.twitter %>" placeholder="Twitter URL">
  <input type="url" name="instagram" value="<%= member.instagram %>" placeholder="Instagram URL">
  <input type="url" name="linkedin" value="<%= member.linkedin %>" placeholder="LinkedIn URL">

    <input type="url" name="imageUrl" value="<%= member.imageUrl %>" placeholder="Fotoğraf URL" required>
    <button type="submit">Güncelle</button>
  </form>
</body>
</html>
