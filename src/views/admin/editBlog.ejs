<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog Düzenle</title>
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #fff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    
    h1 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 1.5rem;
      color: #4caf50;
    }
    
    form {
      background-color: #1f1f1f;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
      width: 100%;
      max-width: 500px;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      align-items: center;
    }
    
    form input[type="text"],
    form textarea,
    form button {
      width: 80%;
      padding: 0.8rem;
      border: none;
      border-radius: 4px;
      font-size: 1rem;
    }
    
    form input[type="text"],
    form textarea {
      background-color: #2c2c2c;
      color: #fff;
    }
    
    form textarea {
      resize: vertical;
      min-height: 100px;
    }
    
    form button {
      background-color: #4caf50;
      color: #fff;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    
    form button:hover {
      background-color: #45a049;
    }
    
    form input[type="text"]::placeholder,
    form textarea::placeholder {
      color: #ccc;
    }
  </style>
</head>
<body>
  <h1>Blog Düzenle</h1>
  <form action="/admin/blogs/edit/<%= blog._id %>" method="POST" class="form">
    <input type="text" name="title" value="<%= blog.title %>" placeholder="Başlık" required>
    <textarea name="content" placeholder="İçerik" required><%= blog.content %></textarea>
    <input type="text" name="author" value="<%= blog.author %>" placeholder="Yazar" required>
    <input type="url" name="imageUrl" value="<%= blog.imageUrl || '' %>" placeholder="Resim URL (isteğe bağlı)">
    <button type="submit">Güncelle</button>
  </form>
</body>
</html>
